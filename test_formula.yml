partitions:
  $name: "HD Partitions"
  $type: "edit-group"
  $minItems: 2
  $maxItems: 3
  $prototype:
    name:
      $default: "some name"
    mountpoint:
      $default: "/root"
    size:
      $type: number
      $name: "Size in GB"
  # $default:
  #   - name: "Boot"
  #     mountpoint: "/boot"
  #   - name: "Root"
  #     mountpoint: "/root"
  #     size: 5000
